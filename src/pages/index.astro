---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Hero from "../components/Hero.astro";
import Partners from "../components/Partners.astro";
import Innovation3D from "../components/Innovation3D.astro";
import QuienesSomos from "../components/QuienesSomos.astro";
import Servicios from "../components/Servicios.astro";
import Proceso from "../components/Proceso.astro";
import Auditoria from "../components/Auditoria.astro";
import Resultados from "../components/Resultados.astro";
import Diferencial from "../components/Diferencial.astro";
import Precios from "../components/Precios.astro";
import Contacto from "../components/Contacto.astro";
import Footer from "../components/Footer.astro";
import Whatsapp from "../components/Whatsapp.astro";
import MaskDivider from "../components/MaskDivider.astro";
---

<Layout title="Loja Nuven — Ingeniería de Crecimiento Digital">
  <Header />

  <main id="smooth-content">
    <Hero />
    <Partners />
    <MaskDivider nextSectionId="#innovation" />
    <Innovation3D />
    <QuienesSomos />
    <Servicios />
    <Proceso />
    <Auditoria />
    <Resultados />
    <Diferencial />
    <Precios />
    <Contacto />
  </main>

  <Footer />
  <Whatsapp />
</Layout>

<style>
  /* Orchestrator level styles if needed */
  main {
    position: relative;
    z-index: 5;
  }
</style>

<script>
  // Global Interactions Orchestrator
  document.addEventListener('astro:page-load', () => {
    // Reveal Animations (GSAP)
    // @ts-ignore
    gsap.registerPlugin(ScrollTrigger);

    // Common reveals for all section titles
    // @ts-ignore
    gsap.utils.toArray('.sec-title').forEach((title: any) => {
      // @ts-ignore
      gsap.from(title, {
        scrollTrigger: {
          trigger: title,
          start: "top 90%",
          toggleActions: "play none none none"
        },
        y: 40,
        opacity: 0,
        duration: 1.2,
        ease: "power3.out"
      });
    });

    // Label pill reveals
    // @ts-ignore
    gsap.utils.toArray('.label-pill').forEach((pill: any) => {
      // @ts-ignore
      gsap.from(pill, {
        scrollTrigger: {
          trigger: pill,
          start: "top 92%",
        },
        scale: 0.8,
        opacity: 0,
        duration: 0.8,
        ease: "back.out(1.7)"
      });
    });
  });
</script>
