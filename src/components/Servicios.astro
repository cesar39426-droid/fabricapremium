---
interface Service {
  icon: string;
  label: string;
  title: string;
  description: string;
  detail: string;
  badge?: string;
  featured?: boolean;
}

interface Props {
  badge?: string;
  title?: string;
  titleGrad?: string;
  subtitle?: string;
  services?: Service[];
}

const {
  badge = "Servicios",
  title = "Todo lo que necesitás para",
  titleGrad = " crecer online",
  subtitle = "Desde la estrategia hasta la ejecución. Un equipo, resultados medibles.",
  services = [
    {
      icon: "🛒",
      label: "E-commerce",
      title: "Tiendas Nube",
      description: "E-commerce con todo integrado. Listo para vender desde el día 1.",
      detail: "Tiendas conectadas a MercadoPago, WhatsApp, Instagram y Google Shopping. UX optimizada para reducir abandonos de carrito hasta un 40%.",
      badge: "Más solicitado",
      featured: true
    },
    {
      icon: "🌐",
      label: "Sitios Web",
      title: "Sitios Ultra-Conversión",
      description: "Páginas que convierten visitas en clientes reales.",
      detail: "Diseño único por cliente. Mobile-first, carga en menos de 2 segundos. Cada visita tiene un camino claro hacia la compra."
    },
    {
      icon: "🔍",
      label: "Posicionamiento",
      title: "SEO & Visibilidad",
      description: "Aparecés donde tu cliente te busca en Google.",
      detail: "Posicionamiento orgánico local y nacional. Resultados medibles en los primeros 90 días."
    },
    {
      icon: "⚡",
      label: "Automatización",
      title: "Marketing con IA",
      description: "Automatización 24/7 — tu vendedor digital que nunca duerme.",
      detail: "Chatbots, email sequences y campañas automáticas que cierran ventas mientras te enfocás en tu negocio."
    }
  ],
} = Astro.props;
---

<section id="servicios" class="sec" aria-labelledby="servicios-title">
  <div class="wrap">
    <div class="sec-head-svc">
      <div class="label-pill">{badge}</div>
      <h2 class="sec-title" id="servicios-title">
        {title}
        <span class="grad-text">{titleGrad}</span>
      </h2>
      <p class="sec-sub-svc">{subtitle}</p>
    </div>

    <div class="bento-grid">
      {services.map((s, i) => (
        <article
          class={`bc-card${s.featured ? ' bc-featured' : ''}`}
          data-index={i}
          role="button"
          tabindex="0"
          aria-expanded="false"
        >
          <span class="bc-lbl">{s.label}</span>
          <div class="bc-icon-wrap">{s.icon}</div>
          <h3 class="bc-title">{s.title}</h3>
          <p class="bc-desc">{s.description}</p>
          <p class="bc-detail">{s.detail}</p>
          {s.badge && <span class="bc-badge">{s.badge}</span>}
          <div class="bc-cta">Ver más <span class="bc-arrow">→</span></div>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .sec-head-svc {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 5rem;
  }

  .sec-sub-svc {
    font-size: 1.1rem;
    color: rgba(255,255,255,0.6);
    margin-top: 1rem;
  }

  .bento-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 28px;
  }

  .bc-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.07);
    border-radius: 24px;
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    transition: all 0.45s cubic-bezier(0.16, 1, 0.3, 1);
    min-height: 340px;
  }

  .bc-card:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(249, 115, 22, 0.5);
    transform: translateY(-6px);
    box-shadow: 0 24px 48px rgba(0,0,0,0.4);
  }

  .bc-card.is-open {
    background: linear-gradient(135deg, rgba(15,23,42,0.98), rgba(249,115,22,0.1));
    border-color: var(--safety-orange);
    transform: scale(1.02);
  }

  .bc-featured {
    grid-column: span 2;
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(249, 115, 22, 0.2);
  }

  .bc-lbl {
    font-size: 0.68rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--safety-orange);
  }

  .bc-icon-wrap {
    font-size: 2.2rem;
    line-height: 1;
    margin: 0.25rem 0;
  }

  .bc-title {
    font-size: 1.5rem;
    font-weight: 800;
    color: #fff;
    margin: 0;
  }

  .bc-desc {
    font-size: 0.95rem;
    color: rgba(255,255,255,0.55);
    line-height: 1.6;
    margin: 0;
    flex-grow: 1;
  }

  .bc-detail {
    font-size: 0.9rem;
    color: rgba(255,255,255,0.75);
    line-height: 1.7;
    margin: 0;
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transition: max-height 0.4s ease, opacity 0.4s ease;
  }

  .bc-card.is-open .bc-detail {
    max-height: 200px;
    opacity: 1;
  }

  .bc-card.is-open .bc-desc {
    display: none;
  }

  .bc-badge {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    background: var(--safety-orange);
    color: #000;
    font-size: 0.65rem;
    font-weight: 900;
    padding: 4px 12px;
    border-radius: 99px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .bc-cta {
    font-size: 0.75rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--safety-orange);
    margin-top: auto;
    padding-top: 1rem;
  }

  .bc-arrow {
    display: inline-block;
    transition: transform 0.3s;
  }
  .bc-card.is-open .bc-arrow {
    transform: rotate(90deg);
  }

  @media (max-width: 1024px) {
    .bento-grid { grid-template-columns: 1fr 1fr; }
    .bc-featured { grid-column: span 2; }
  }

  @media (max-width: 640px) {
    .bento-grid { grid-template-columns: 1fr; }
    .bc-featured { grid-column: span 1; }
  }
</style>

<script>
  document.addEventListener('astro:page-load', () => {
    document.querySelectorAll('.bc-card').forEach(card => {
      const toggle = () => {
        const isOpen = card.classList.toggle('is-open');
        card.setAttribute('aria-expanded', String(isOpen));
        const cta = card.querySelector('.bc-cta');
        if (cta) cta.innerHTML = isOpen ? 'Resumir <span class="bc-arrow">→</span>' : 'Ver más <span class="bc-arrow">→</span>';
      };
      card.addEventListener('click', toggle);
      card.addEventListener('keydown', (e: any) => { if (e.key === 'Enter' || e.key === ' ') toggle(); });
    });
  });
</script>
